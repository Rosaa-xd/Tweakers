@model List<Tweakers.Models.Category>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<div>
    <ul class="categories">
        @foreach (var c in Model)
            {
            <li>@Html.ActionLink(c.Name, "Cat" + c.ID, "Category")</li>
        }
    </ul>
</div>